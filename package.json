{
  "name": "wikimoe-bookkeeping",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:server": "node --watch --experimental-sqlite --env-file=.env server/src/app.js",
    "start": "node --experimental-sqlite --env-file=.env server/src/app.js",
    "build": "cd web && npm run build"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
